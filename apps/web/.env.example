# Base URL of the Directus instance the Next.js site reads from.
# Public (NEXT_PUBLIC_) because it is also used for client-side asset URLs.
# Local dev example:  http://localhost:8055
# Production example:  https://api.your-domain.ru
NEXT_PUBLIC_DIRECTUS_URL=http://localhost:8055
