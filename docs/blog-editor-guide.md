# Блог ISVOI в Directus Studio

Блог строится как экспертная библиотека о выборе, диагностике и владении
техникой. Это не новостная лента и не универсальный page builder: редактор
работает со структурированными материалами, а публичную верстку контролирует
Next.js.

## Коллекции

- `Блог · Материалы` (`blog_posts`) — статьи и их публикационный статус.
- `Блог · Рубрики` (`blog_categories`) — несколько устойчивых верхнеуровневых
  тем.
- `Блог · Теги` (`blog_tags`) — более точные тематические признаки.
- `Блог · Авторы` (`blog_authors`) — публичные авторы и эксперты.
- `Блог · Связи тегов` и `Блог · Связи устройств` — системные junction-таблицы,
  скрытые из обычной навигации Studio.
- `ISVOI Blog` в Directus Files — обложки, портреты и иллюстрации статей.

## Статусы материала

1. `draft` — рабочий черновик, не виден на сайте.
2. `review` — текст и факты готовы к редакторской проверке.
3. `scheduled` — материал одобрен и имеет будущую дату `publish_at`.
4. `published` — материал публичен, если `published_at` уже наступила.
5. `archived` — снят с основной публикации без удаления истории.

Начинайте работу с пресетов `Черновики`, `На проверке`, `Запланированные`,
`Опубликованные` и `Неполные материалы`.

## Создание статьи

1. Сначала выберите или создайте рубрику и автора.
2. Создайте материал со статусом `draft`, заполните заголовок и `slug`.
3. Добавьте лид и основной текст. Используйте короткие абзацы, H2/H3, списки,
   цитаты и обычные ссылки; не вставляйте стили, скрипты или готовую HTML-сетку.
4. Загрузите обложку в папку `ISVOI Blog` и заполните осмысленный alt-текст.
5. Выберите одну рубрику, автора и только действительно полезные теги.
6. При необходимости свяжите статью с опубликованными устройствами каталога.
7. Проверьте SEO title, meta description, canonical и social image.
8. Переведите материал в `review`. Публикуйте только после проверки фактов,
   орфографии, ссылок, изображений и мобильного отображения.

## Правила публикации

Опубликованный материал должен иметь:

- заголовок, уникальный slug, лид и основной текст;
- обложку и alt-текст;
- активную рубрику и активного автора;
- `published_at`, которая не находится в будущем;
- проверенные внутренние и внешние ссылки;
- meta description без технического текста и обещаний, которых нет в статье.

Не меняйте slug опубликованной статьи без redirect в Next.js. Не удаляйте
материалы, рубрики, теги или авторов в обычной работе: используйте `archived` и
`is_active`. Связи тегов и устройств можно удалять из карточки материала.

## Setup и аудит

Генератор идемпотентной схемы и Studio metadata:

```bash
npm run directus:setup:blog
npm run directus:setup:blog-scheduling
BLOG_PREVIEW_SECRET=<server-secret> npm run directus:setup:blog-preview
```

Первый QA-черновик можно создать идемпотентно, передав UUID уже загруженной
обложки. Скрипт не публикует материал и не перезаписывает редакторские правки
при повторном запуске:

```bash
BLOG_PILOT_COVER_FILE_ID=<directus-file-uuid> npm run directus:seed:blog-pilot
```

Read-only аудит после применения SQL:

```bash
npm run directus:audit-blog
```

Setup только печатает SQL. На production его применяют через PostgreSQL
контейнер Directus в рамках отдельного согласованного выката. До готовности
Next.js routes не добавляйте ссылку `/blog` в `navigation_items` и не создавайте
публичные материалы.

## Rollout

1. Применить `directus:setup:blog`, `directus:setup:blog-scheduling` и
   `directus:setup:blog-preview`. Для preview в Next.js должны быть заданы
   `DIRECTUS_PREVIEW_TOKEN` и тот же `BLOG_PREVIEW_SECRET`; значения не
   коммитятся. Token должен принадлежать отдельному service user с policy
   `ISVOI Blog Preview`. В nginx location `/api/draft/blog` отключается access
   log, чтобы одноразовый по назначению secret не сохранялся вместе с query.
2. Выполнить `directus:audit-blog` и сохранить schema snapshot.
3. Развернуть Next.js маршруты `/blog`, `/blog/[slug]`, рубрики и RSS.
4. Создать тестовые рубрику, автора и один материал в `draft`.
5. Проверить preview, затем публикацию и revalidation.
6. Только после smoke-проверки добавить `Блог` в header/footer navigation.

`blog_posts` использует Directus Content Versioning. Live Preview открывает
защищенный Next.js Draft Mode и читает черновик отдельным server-only token.
CRON Flow запускается раз в минуту и меняет статус на `published` только у
полных записей с наступившей `publish_at`; database trigger заполняет
`published_at`, а event Flow сразу снимает cache tags.
